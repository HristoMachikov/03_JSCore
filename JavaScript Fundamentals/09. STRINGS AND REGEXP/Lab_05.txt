value type			ref. type
stack				heap
string- value type

let newNumber = new Number(5.55) - създава обект;

newNumber == 5.55 -> true
взима valueOf функцията на обекта  и връща примитивния тип (динамично)

Coersion операции невидими за нас рапва стойности, когато имаме 2=
При 3= това се забванява

let arr2 = [1, 2, 3]; ->ref.type
let arr3 = [1, 2, 3]; ->ref.type
arr2 === arr3 -> false (сравнява референцията)
arr2 == arr3 -> false (сравнява референцията)

C# - select()	all	any	substring()	--------	foreEach
JS - map()	every	some	substr()	substring()	for(elem of arr)	


evry връща true/false
indexOf() = search()
search() - по-бавен, работи с RegEx, връща индекса
filter() - намалява масива, връща true/false

camalCase - azSymBylgarche
PascalCase - AzSymBylgarche

спред оператор (...number) взима масива и го подава като отделни елементи

regexr.com
? 0 или едно
+ едно или повече
* 0 или повече
w = [A-Za-z0-9_]
. - хваща всичко след нея
*? - 0 или безброи колкото може по-малко пъти - lazi
.* - 0 или безброи колкото може повече пъти - grady
^, - всичко различно от ,

групата реферира стоиноста а не патерна

patern.exec(string)


["enCode", "Some messages are just not encoded what can you do? ",
"RE - ENCODE THEMNOW! - he said. ", "Damn encode, ITSALLHETHINKSABOUT, eNcoDe BULL$#!%."]