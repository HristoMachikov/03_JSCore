Функцията се закача към неиния прототип(prototype на обекта)

преди ES2015 (ES6)
function Rectanglethis.width height) {
 this.width = this.width 
 this.height = height
}
Rectangle.prototype.area = function () {
  return this.width * this.height;
}

аксесора гетър(get) е пропарти на класа
валидация на данни се прави през сетъра(set) 

когато се дефинират пропартита минават през get

kogato se promenqt минават през set

https://developer.mozilla.org/en-US/docs/Web/JavaScript/Inheritance_and_the_prototype_chain

при наследяване
extends - закача за прототипа на новия клас всички методи
super - закача за прототипа на новия клас всички propatrty-та
при абстракция
абстрактен клас не се инстанцира само се наследява
пише се проверка в конструктура
if(new.target === Person){
throw new Error ("Abstract class");
}

mocha test exploler
module.exports = {StringBuilder, Person}

--Constructur
---Can be instantiated with a passed in string 
---Can be instantiated without a passed in string 
--Append
---test if the param is a string(if not throw error)
---test with correct information
--Prepend
---test with correct information
--Insert at
---test if the param is a string(if not throw error)
--- test with a string
--- test with a single character
--Remove