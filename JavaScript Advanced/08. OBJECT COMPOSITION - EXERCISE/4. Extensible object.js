function solve() {
    let myObj = {
        extend: (template) => {
            const entries = Object.entries(template)
            for (){
                
            }
        }

    }
    return myObj;
}