Асинхронния код се делегира за изпълнение във времето

Patterns за инплементиране на асинх.код CallBack, Promises, Async/Await

Метод then на Promises  се изпълнява след като се извика resolve() метода
Метод catch на Promises  се изпълнява след като се извика reject() метода

const myPromise = new Promise (() =>{});   

Ние задаваме кога и къде да се извикват callBack на промиса

всяка нова стоиност от промиса се рапва в нов промис
return Promise.resolve(value); ===
return value;

async се слага преди метода коти рапва
await се слага преди функцията която вика промис (.then())

.json() === JSON.Parse

caniuse.com